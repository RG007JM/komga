# Kobo integration documentation

These pages document the Kobo-specific extensions in this Komga fork. They describe this fork's source-code behavior; they are not upstream Komga API documentation.

- [Kobo Store ISBN lookup](KOBO_STORE_ISBN_LOOKUP.md) — discovery routes, verification, Japanese edition bridging, persistence, regressions, integration points, and source-linked tests.

The separate Komga API document can be added here as `KOMGA_API.md` when available; before linking it, check that any source-relative links use the correct path from `docs/kobo/` (for example `../../komga/src/...`).
