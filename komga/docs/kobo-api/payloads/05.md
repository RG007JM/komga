# 8.5. `/v1/library/tags`

[← Payload index](README.md) · [Endpoint reference](../reference.md)

### 8.5. `/v1/library/tags`

**Observed device methods:** POST. **Capture sources:** 1 parsed collection-creation request in the original device trace; the separately observed response is HTTP `201` with a JSON-string collection ID. The later recommendation-feedback request belongs solely to §8.19.

**DEVICE request — `POST https://komga.com/kobo/{token}/v1/library/tags`**, `device:L35153`. Full captured request body:

```json
{
  "Items": [
    {
      "RevisionId": "0RDTATM01HY4H",
      "Type": "ProductRevisionTagItem"
    }
  ],
  "Name": "testing"
}
```

**DEVICE creation response — JSON string**, HTTP `201`, `device:L35173`. The e-reader logger records this primitive as `Not really json: ""...""`; its value is shown below as a valid JSON string, **not** an object or array. This block is a faithful re-serialization of that logged scalar, not a complete raw packetdump.debug body.

```json
"0RPN2SADWHMRB"
```

