# 8.6. `/v1/library/tags/{id}`

[← Payload index](README.md) · [Endpoint reference](../reference.md)

### 8.6. `/v1/library/tags/{id}`

**Observed device methods:** PUT. **Capture sources:** 1 device request(s) containing parsed JSON, 0 upstream response(s) containing parsed JSON.

**DEVICE request — `PUT https://komga.com/kobo/{token}/v1/library/tags/0RPN2SADWHMRB`**, `device:L36704`. Full captured request body:

```json
{
  "Name": "testing2"
}
```

**DEVICE response JSON:** no complete, parseable body available for this route in that source.

**UPSTREAM response JSON:** no complete, parseable body available for this route in that source.

**Endpoint-specific data gaps:** No upstream response in the supplied server log. No complete device JSON response for this route. For other inputs, headers, errors and optional fields, do not infer unobserved values.

