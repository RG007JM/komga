# 8.18. `/v1/user/reviews`

[← Payload index](README.md) · [Endpoint reference](../reference.md)

### 8.18. `/v1/user/reviews`

**Observed device methods:** GET. **Capture sources:** 4 device request(s) containing parsed JSON, 4 upstream response(s) containing parsed JSON.

**DEVICE request JSON:** no request body captured for this route. This is normal for GET, but is not proof of a universal empty-body contract.

**DEVICE response, structure variant 1** — HTTP `200`, `device:L27345`, object (3 top-level keys). Actual captured URL: `https://komga.com/kobo/{token}/v1/user/reviews?ProductIds=0RDTATKHXHPS9`.

```json
{
  "CurrentPageIndex": 1,
  "Items": [],
  "TotalPageCount": 0
}
```

*3 additional response(s) share this structural shape; individual, complete JSON bodies are preserved in the all-captures ledger and ZIP.*

**UPSTREAM response, structure variant 1** — HTTP `200`, `server:L312`, object (3 top-level keys). Actual captured URL: `https://storeapi.kobo.com/v1/user/reviews?ProductIds=8201afa9-c23b-429b-a642-a4bcf1c8b638`.

```json
{
  "Items": [],
  "TotalPageCount": 0,
  "CurrentPageIndex": 1
}
```

*3 additional response(s) share this structural shape; individual, complete JSON bodies are preserved in the all-captures ledger and ZIP.*

**Translation boundary:** this upstream body was logged before Komga could overlay local IDs/metadata. Compare with DEVICE response where available. Matching route names alone do not establish that the two examples came from the same request.

**Endpoint-specific data gaps:** No device request JSON body captured. For other inputs, headers, errors and optional fields, do not infer unobserved values.

