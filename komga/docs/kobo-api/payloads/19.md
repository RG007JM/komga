# 8.19. `/v1/user/recommendations/feedback` — both captured request bodies, device response, and translation boundary

[← Payload index](README.md) · [Endpoint reference](../reference.md)

### 8.19. `/v1/user/recommendations/feedback` — both captured request bodies, device response, and translation boundary

**Two separately dated observations:** The original `Full - Copy.redacted.txt` + `komga.redacted.txt` trace shows a one-item POST containing an untranslated Komga ID blocked by `KoboOutboundRequestGuard` (`komga:L1260–1262`). The later `Review_v2.redacted.txt` device trace shows a **different, two-item POST** receiving a device-facing **204 No Content**, with no JSON response body (`review_v2:L9290–9330`). No matching later Komga outbound-server log was supplied: the 204 is confirmed at the device/Komga boundary, **not** proof of translation or upstream forwarding. Do not merge these two transactions or invent a Kobo Store success status.

**Observed device method:** `POST`. **Captured evidence:** two distinct device request JSON bodies (original: one item; later: two items), a later device-facing HTTP `204 No Content` with no response entity, original server-side guard rejection, and no paired later upstream exchange.

**DEVICE request — `POST https://komga.com/kobo/{token}/v1/user/recommendations/feedback`**, `device:L33099`. Full captured request body:

```json
{
  "FeedbackItems": [
    {
      "FeedbackType": "Rate5Star",
      "RevisionId": "0RDTATKHXHPS9"
    }
  ]
}
```

**LATER DEVICE request — 17:57:44 local time (`review_v2:L9293–9313`):** Full captured JSON body, two feedback items:

```json
{
  "FeedbackItems": [
    {"FeedbackType": "Rate4Star", "RevisionId": "0RE3XW4NEEGG4"},
    {"FeedbackType": "Rate4Star", "RevisionId": "0RDTATKFHHSYW"}
  ]
}
```

**LATER DEVICE response (`review_v2:L9314–9330`):** HTTP `204 No Content`, `Content-Length` absent from the response header log, **zero response-body bytes**; there is no JSON value to parse. An empty wire body is not `{}`, `[]`, `null`, or the JSON string `""`. `SubmitFeedbackCommand` then finishes (`review_v2:L9331–9334`). The earlier blocked request and this later success are not the same transaction.

**UPSTREAM response JSON:** none provided for this endpoint, even in the newer capture; whether the later successful device-facing request was forwarded and exactly which Kobo identities it contained remains **unverified**. HTTP 204 cannot establish the upstream service identity or confirm a `RevisionId` ↔ `ProductId` mapping.

**Translation contract to verify in a paired server capture:** for every input `FeedbackItems[i].RevisionId`, preserve `FeedbackType`, list order, and number of elements; resolve an eligible local book identity to the **identifier type expected by this feedback endpoint**, not blindly to an arbitrarily related `ProductId`. Apply the outbound guard after any verified translation. The current source archive describes the original fork, not an asserted patch for the later run.

**Original request outcome:** the paired original Komga server log records a rejected outbound request containing an untranslated local Komga ID. No successful upstream result is shown for that original request.

**Other status codes and failure bodies:** not observed for a successfully forwarded upstream feedback POST. The later device `204` is not proof of upstream forwarding.

**Endpoint-specific data gaps:** Neither a paired later Komga-server capture showing the translated outbound feedback body nor a matched Kobo Store response was supplied. Do not infer either from the device-facing `204`.

