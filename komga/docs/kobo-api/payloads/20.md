# 8.20. `/v1/user/wishlist/items`

[← Payload index](README.md) · [Endpoint reference](../reference.md)

### 8.20. `/v1/user/wishlist/items`

**Observed device methods:** POST. **Capture sources:** 1 device request(s) containing parsed JSON, 0 upstream response(s) containing parsed JSON.

**DEVICE request — `POST https://komga.com/kobo/{token}/v1/user/wishlist/items`**, `device:L45735`. Full captured request body:

```json
{
  "Add": [
    {
      "CrossRevisionId": "7aad241f-f35f-36d7-bbe4-32dc10b65e9f",
      "DateAdded": "2026-09-22T11:53:24Z"
    }
  ],
  "Remove": []
}
```

**DEVICE response, structure variant 1** — HTTP `200`, `device:L45756`, object (1 top-level keys). Actual captured URL: `https://komga.com/kobo/{token}/v1/user/wishlist/items`.

```json
{
  "SuccessfulIds": [
    "7aad241f-f35f-36d7-bbe4-32dc10b65e9f"
  ]
}
```

**UPSTREAM response JSON:** no complete, parseable body available for this route in that source.

**Endpoint-specific data gaps:** No upstream response in the supplied server log. For other inputs, headers, errors and optional fields, do not infer unobserved values.

