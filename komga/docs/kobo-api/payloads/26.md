# 8.26. `/v1/analytics/gettests`

[← Payload index](README.md) · [Endpoint reference](../reference.md)

### 8.26. `/v1/analytics/gettests`

**Observed device methods:** POST. **Capture sources:** 5 device request(s) containing parsed JSON, 5 upstream response(s) containing parsed JSON.

**DEVICE request — `POST https://komga.com/kobo/{token}/v1/analytics/gettests`**, `device:L477`. Full captured request body:

```json
{
  "AffiliateName": "Kobo",
  "ApplicationVersion": "4.46.23836",
  "PlatformId": "00000000-0000-0000-0000-000000000390",
  "SerialNumber": "[REDACTED]",
  "TestKey": "00000000-0000-4000-8000-000000000000"
}
```

**DEVICE response, structure variant 1** — HTTP `200`, `device:L498`, object (3 top-level keys). Actual captured URL: `https://komga.com/kobo/{token}/v1/analytics/gettests`.

```json
{
  "Result": "Success",
  "TestKey": "00000000-0000-4000-8000-000000000000",
  "Tests": {}
}
```

*4 additional response(s) share this structural shape; individual, complete JSON bodies are preserved in the all-captures ledger and ZIP.*

**UPSTREAM response, structure variant 1** — HTTP `200`, `server:L32`, object (3 top-level keys). Actual captured URL: `https://storeapi.kobo.com/v1/analytics/gettests`.

```json
{
  "Result": "Success",
  "TestKey": "00000000-0000-4000-8000-000000000000",
  "Tests": {}
}
```

*4 additional response(s) share this structural shape; individual, complete JSON bodies are preserved in the all-captures ledger and ZIP.*

**Translation boundary:** this upstream body was logged before Komga could overlay local IDs/metadata. Compare with DEVICE response where available. Matching route names alone do not establish that the two examples came from the same request.

**Endpoint-specific data gaps:** For other inputs, headers, errors and optional fields, do not infer unobserved values.

