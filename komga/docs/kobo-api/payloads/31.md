# 8.31. `/v1/user/wishlist`

[← Payload index](README.md) · [Endpoint reference](../reference.md)

### 8.31. `/v1/user/wishlist`

**Observed device methods:** not in device capture. **Capture sources:** 0 device request(s) containing parsed JSON, 8 upstream response(s) containing parsed JSON.

**DEVICE request JSON:** no request body captured for this route. This is normal for GET, but is not proof of a universal empty-body contract.

**DEVICE response JSON:** no complete, parseable body available for this route in that source.

**UPSTREAM response, structure variant 1** — HTTP `200`, `server:L9`, object (8 top-level keys). Actual captured URL: `https://storeapi.kobo.com/v1/user/wishlist?PageSize=100&PageIndex=0`.

```json
{
  "TotalCountByProductType": {},
  "Items": [],
  "ItemCount": 0,
  "TotalPageCount": 0,
  "TotalItemCount": 0,
  "CurrentPageIndex": 0,
  "ItemsPerPage": 100,
  "VersionCode": 2
}
```

*7 additional response(s) share this structural shape; individual, complete JSON bodies are preserved in the all-captures ledger and ZIP.*

**Translation boundary:** this upstream body was logged before Komga could overlay local IDs/metadata. Compare with DEVICE response where available. Matching route names alone do not establish that the two examples came from the same request.

**Endpoint-specific data gaps:** No complete device JSON response for this route. No device request JSON body captured. For other inputs, headers, errors and optional fields, do not infer unobserved values.

